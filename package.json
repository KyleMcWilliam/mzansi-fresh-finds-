{
  "name": "mzansi-fresh-finds",
  "version": "1.0.0",
  "description": "A web application that connects users with local South African businesses offering deals on near-dated food items.",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "test": "concurrently \"npm test --prefix backend\" \"npm test --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
